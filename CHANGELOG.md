# Changelog

Significant changes only (CLAUDE.md rule 5): one line per change, 1–2
sentences — essence and intent, not content.

- 2026-07-09: Adopted the Kiosk 0.1 convergence process: repo constitution in CLAUDE.md (five rules), workspace-level findings/TODO ledgers, and a repo-wide CI workflow running every gem suite, demo happy-path/isolation/redteam flows, and e2e. (PLAN.md at the workspace root)
- 2026-07-09: Discovery `/.well-known/kiosk.json` now advertises `capabilities` as verb names (schema/query/run/pay) computed from the registry, replacing the static `[query,actions,ap2]`. (ADR-0009, K-017)
- 2026-07-09: Registration accepts an optional provider `assistant_creation` factory so hosts with a validated account model no longer 500 on agent sign-up. (ADR-0010, K-029)
- 2026-07-09: AP2 mandate verification now requires all six spec-mandated claims (id, user_id, agent_id, iss, iat, exp), not exp alone — a mandate missing id/iat is rejected. (K-020)
- 2026-07-09: Corrected gem READMEs and the equihash PoW cost claims to shipped reality (kiosk-server shipped; equihash is the default; canonical N×PoW reputation wire; honest ~16 ms/few-KB verify). (K-004..K-008, K-033)
- 2026-07-10: Registration contract switched to block-returns-id — assistant_creation returns the new record's id, so bigint and uuid apps both work; agents.public_key gains a partial UNIQUE index against double-register races. (ADR-0010, K-029, K-043; retroactive entry per K-073)
- 2026-07-10: Demos self-provision an ephemeral dev signing key when none is set, so every demo runs out of the box. (K-034, T-016; retroactive entry per K-073)
- 2026-07-10: saas-booking's OAuth device-grant walkthrough reframed as the optional existing-account path. (K-016; sanctioned by the ADR-0008 amendment; retroactive entry per K-073)
- 2026-07-11: Auth surface per ADR-0011/0012 — registration_role is optional and role-less agents work end-to-end (no role claim, no role GUC); invalid/expired/revoked tokens get 401 instead of 500; the KYC endpoint honors the configured IdP; the device grant rejects undeclared roles; the advertised skill URL is the immutable versioned file. (K-031, K-070, K-071, K-072, K-078, K-050)
- 2026-07-11: kiosk-core drops its unwired extension seams — CredentialBrokers, NotificationAdapter, and the Event value type — until a shipped consumer needs them. (K-068, K-069)
- 2026-07-11: Demos re-pin the skill integrity hash to the immutable skill-v0.1.1.md (pin drift now fails the test-support suite in a local multi-repo checkout); every stub-PSP demo registers payment_setup; skooti's per-demo skill teaches the real Equihash 402 wire; saas-booking gets per-demo database names and drops the nonexistent CLI pitch. (ADR-0012, K-050, K-057, K-067, K-048, K-010)
- 2026-07-11: READMEs and CI aligned with shipped reality — Equihash default everywhere, opt-in-RLS wording, no unshipped rake tasks or fraud-detection claims; CI runs demo:schema on all five demos and redteam on saas-booking; dependabot points at the real bundler roots. (K-006, K-063, K-064, K-065, K-049, K-035)
- 2026-07-11: Identity plane defined (ADR-0013) — the bundled kiosk-pop engine is the default agent-IdP so zero-config installs verify what they mint; the wire falls through to user_idp by verification result; KYC is agent-only; custom-adapter token issuance is deferred to the 0.2 seam. (K-082)
- 2026-07-12: Mandate verification normalizes the principal check to string (bigint-PK hosts no longer 403 every mandate) and rejects cross-currency carts against an intent cap; the KYC endpoint returns 400 not 500 on a malformed body; the unimplemented `events` verb was removed (unknown verbs return a clean error); the auth-challenge TTL default now outlives the registration PoW solve window; the advertised default skill URL bumps to skill-v0.1.2.md. (K-092, K-101, K-093, K-083, K-100, K-050)
- 2026-07-12: Demos re-pin the skill integrity hash to skill-v0.1.2.md; saas-booking's queries/actions now carry schema descriptions; skooti's narrative PoW references corrected to the shipped Equihash gate and the nonexistent kiosk-credentials-* KYC gem replaced with the real verified-attestation path. (K-050, K-099, K-088, K-084)
- 2026-07-13: saas-booking's `_test` device-authorization fixture routes and its bin/demo device-grant walkthrough are retired — no test-only HTTP surface ships in a demo; the README now presents the mounted OAuth endpoints as dormant (ADR-0008) and defers human↔agent account-linking past 0.1. (K-156, T-020)
- 2026-07-12: READMEs/gemspecs scrubbed of superseded design-spec section citations and the removed kiosk-credentials-* family; dropped the last "production-grade" overclaim; CI runs each demo task in its own invocation and gives getgrocery a dummy Stripe key so its boot guard passes. (K-024, K-084, K-089, K-090, K-086, K-087)
- 2026-07-12: getgrocery's demo:shop self-starts a local stripe-mock when no Stripe key is present, so CI runs the full register→pay→confirm flow (including the off_session charge) secret-free — no test keys in CI. (K-110)
- 2026-07-12: Payment is agent-only — an agentless caller (user_idp web/mobile session, no agent_id) now gets a clean 403 on POST /pay instead of a 500 deep in mandate verification; the hoteling demo drops its inert copy-paste KYC route (no key configured). (K-114, K-113)
- 2026-07-12: WireController now returns 400 bad_request (not an uncaught 500) on a malformed wire body, and Equihash.verify returns false (not raises) on a malformed proof index — two parse/validate-outside-guard robustness gaps found by the Phase-4 line audit. (K-147, K-149)
- 2026-07-12: foodelivery and hoteling demo skills teach the real Equihash 402 PoW wire (were instructing the retired Argon2id solver, so an agent following them could not register). (K-125)
- 2026-07-12: Phase-4 line-audit cleanup — removed orphaned demo bin/demo scaffolding and dead e2e device-verify fixtures; scrubbed PoW/reputation/redteam docstrings, gemspecs and demo before-after snapshots of retired-Argon2id and pre-retune wording so shipped docs match the shipped Equihash reality; added coverage for revoke, malformed bodies and other error paths. (K-116..K-153)
- 2026-07-12: KYC attestation verification normalizes the subject check to string, so a bigint-PK host no longer 403s every KYC attestation — the same string-principal fix already applied to mandate verification, mirrored to its KycVerifier sibling. (K-092)
- 2026-07-12: Phase-4 pass#2 fix wave — removed dead payment-adapter contract seams (Base#authorize/#refund, Stripe#refund, Mandate::Settlement, Result#query_id) and the demos' dead _test device-grant routes; the e2e harness now proves per-user app-layer isolation with a genuine cross-user negative case (Alice/Bob each see only their own rows); added AuthController and cuckoo target-path coverage; and scrubbed PoW/reputation/redteam/rls/demo docstrings, gemspecs, changelogs and the getgrocery worked example to shipped reality. (K-155..K-182 + reopened K-008/099/122/123/138/142/144/152)
- 2026-07-14: Phase-4 pass#3 fix wave A (real bugs) — mandate verification now rejects a chain that omits a required amount field (an absent total/amount nil-coerced to 0 and settled a 0-cent order) and returns 403 not 500 for a keyless agent; register/login return 400 not 500 on a non-String public_key and the unauthenticated challenge store is bounded; the generated mandates migration rolls back cleanly (drops settlements before its FK parent); Equihash/Cuckoo verify return false rather than raise on malformed proofs; the Devise user-IdP adapter actually authenticates through the wire (reads the request Warden user); foodelivery's showcase walkthrough is fixed and asserts (CI now runs its settling demo:order); and the scooter-specific RentalTokenIssuer moved out of the neutral core into the skooti demo. (K-199, K-200, K-204, K-210, K-137, K-149, K-227, K-155, K-247, K-262, K-273, K-263, K-207)
- 2026-07-14: Phase-4 pass#3 fix wave B (honesty/dead-surface tail, 84 rows across every gem) — docstrings, READMEs, CHANGELOGs, gemspecs, SKILL fragments and demo narratives across core/server/PoW/reputation/redteam/pay-stripe/rls/test-support/e2e/demos were reconciled to shipped reality (only-shipped-gem naming per K-015, live kiosk-pop IdP attribution, real schema/task inventories, the shipped SetupIntent Stripe model, the kiosk.tech origin), a batch of spec-only dead constants/fields/branches was removed, and small coverage gaps (Identity claims-nil, NullExecutor queued results, count-curve edge, tamper_token branches, spec-DSL matchers) were filled. (K-083, K-132, K-138, K-143, K-153, K-190..K-291 minus the wave-A/T-011 rows)
- 2026-07-14: foodelivery's pay is now bound to a placed order — a new confirm_order step gates on order-ownership and a settlement bound to that order (mirroring getgrocery's schedule_delivery), so a principal cannot confirm another's order; the demo's isolation flow now proves this cross-principal 403 instead of skipping it. (K-185)
