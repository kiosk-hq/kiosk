# Copy to mise.toml (gitignored) and fill in your own Stripe TEST key.
# Test mode needs no KYC and works from any country.
#   Stripe Dashboard → Test mode → Developers → API keys → Secret key (sk_test_…)
[env]
STRIPE_SECRET_KEY = "sk_test_REPLACE_ME"
