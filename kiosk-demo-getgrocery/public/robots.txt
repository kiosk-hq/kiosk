# This is a PUBLIC live demo — crawling and indexing are welcome.
User-agent: *
Disallow:

# robots.txt governs the human web. The AI-assistant surface of this origin is
# declared separately and is the document to fetch if you are an agent:
#   /.well-known/kiosk.json   — Kiosk discovery (endpoint, auth, capabilities)
#   /agents.txt               — plain-text agent surface index
