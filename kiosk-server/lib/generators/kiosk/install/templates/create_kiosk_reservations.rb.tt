# frozen_string_literal: true

# Migration 003 — kiosk.reservations for atomic reserve-then-pay.
class CreateKioskReservations < ActiveRecord::Migration[ActiveRecord::Migration.current_version]
  def up
    execute Kiosk::Server::SchemaDefinitions.reservations_sql(
      schema:       <%= options[:schema].inspect %>,
      user_id_type: :<%= options[:user_id_type] %>,
    )
  end

  def down
    execute %(DROP TABLE IF EXISTS "<%= options[:schema] %>".reservations)
  end
end
