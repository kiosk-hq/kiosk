# frozen_string_literal: true

# Convenience entry — `require "kiosk-user-idp-devise"` mirrors the gem name.
# Most callers will instead `require "kiosk/user_identity_providers/devise"`
# directly from their initializer.
require "kiosk/user_identity_providers/devise"
